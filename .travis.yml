

sudo: required

language: node_js

node_js:
 - "8"

services:
 - docker

before_install:
 - docker build -t rakeshz12/docker-react -f Dockerfile.dev .

script: 
 - docker run rakeshz12docker-react npm run test -- --coverage


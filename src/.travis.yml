language: generic 

sudo: required 

services:
  - docker

 before_install:
  - docker build -t rakeshrocky58008/docker-react -f dockerfile.dev .


script:
 - docker run -e CI=true rakeshrocky58008/docker-react npm run test -- --coverage